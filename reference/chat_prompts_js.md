# Generate JavaScript for suggested prompt clicks

Creates the JS code to handle clicking on prompt chips.

## Usage

``` r
chat_prompts_js(container_id)
```

## Arguments

- container_id:

  The DOM ID of the container element

## Value

A shiny tags\$script element
