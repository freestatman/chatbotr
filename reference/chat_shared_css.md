# Shared CSS for chat components

Returns the CSS styles shared between floating and offcanvas chat
modules.

## Usage

``` r
chat_shared_css(include_dark_theme = FALSE)
```

## Arguments

- include_dark_theme:

  Whether to include dark theme variants (default: FALSE)

## Value

A shiny tags\$style element
