% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/settings_module.R
\name{api_settings_server}
\alias{api_settings_server}
\title{API Settings Server Module}
\usage{
api_settings_server(id, default_system_prompt = NULL)
}
\arguments{
\item{id}{Namespace ID matching the UI module}

\item{default_system_prompt}{Default system prompt for the LLM (optional)}
}
\value{
A reactive list containing:
\itemize{
\item client: Configured ellmer client (reactive)
\item provider: Selected provider (reactive)
\item model: Selected model (reactive)
\item is_configured: Whether settings are valid (reactive)
}
}
\description{
Server logic for API settings configuration. Handles provider selection,
API key validation, model selection, and client creation.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  settings <- api_settings_server("settings")

  # Use the configured client
  observe({
    if (settings$is_configured()) {
      client <- settings$client()
      # Use client with shinychat
    }
  })
}
}
}
